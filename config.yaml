name: "Tome for Home Assistant"
description: "A self-hosted ebook library server that knows how you read -- not just what you own.  As a Home Assistant App."
version: "0.1.2"
slug: "tome_for_ha"
init: false
arch:
  - aarch64
  - amd64
ports:
  8080/tcp: 8080
webui: http://[HOST]:[PORT:8080]
map:
  - share:rw
options:
  tome_hardcover_token: ""
schema:
  tome_hardcover_token: str?